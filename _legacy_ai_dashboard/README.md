# Legacy AI Dashboard Quarantine

This folder contains deprecated root-app AI dashboard code moved out of active runtime.

Quarantined items include:
- old root `app/page.tsx`
- old root AI price endpoints (`forecast`, `commodities`, `market`, `indian-markets`, `model-leaderboard`, `price-intel`, `jobs`)
- old dashboard components and `lib/forecast.ts`
- `ALGORITHM_DOCUMENTATION.md`

Do not import from this folder in active apps.
